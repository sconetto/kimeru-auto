{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "if [ -n \"$DATABASE_URL\" ]; then npm run db:migrate:prod || exit 1; fi && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install"
}
